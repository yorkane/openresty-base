# your-production-host.example 部署记录

## 2026-08-25 盘点

- 主机：Ubuntu 22.04.3，x86_64，Docker Engine 28.3.2，Docker Compose v2.38.2。
- 目标机已有 `opencode-ada`（`openagent:latest`）容器；容器映射的主要应用入口为 3210，但本次盘点时从目标机 `127.0.0.1:3210` 请求失败。
- 目标机本次盘点可观察到：8083 返回 HTTP 302，5081 返回 HTTP 502；这些服务状态需在部署后复核。
- 目标机磁盘根分区约 1 TB，已用约 216 GB，可用约 749 GB。

## 本机待复刻服务

- Authz Gateway：host 网络，HTTP 6080、HTTPS 6443，代码/管理端/Lua/模板采用挂载目录。
- 当前域名绑定目标端口：2077、2078、3080、3737、8082；另有 `your-production-host.example:3210` 的远程绑定。
- 本机 Docker 服务：`imgproxy_plus_local`（8082）和 `postgres`（5432）。
- 本机非 Docker 服务：code-server 2077、Pi 应用 2078、DeepSeek Web 3080、OpenCodex Gateway 3737。

## 待完成

- 确认目标机 3210/相关应用的实际启动方式和健康入口。
- 将网关及必要服务部署到目标机，保持 host 网络和持久化数据。
- 通过 APISIX-W 配置 `*-o.ws.example.com` 到目标机入口，并验证本机直连、APISIX 域名、认证、代理和 WebSocket。

## 2026-08-25 第二次核验

- 目标机 `opencode-ada` 容器内部及宿主机曾观察到 3210 监听，但从宿主机 `127.0.0.1:3210` 请求当前仍返回 connection refused；需要修复或重启该容器后再把它作为可用上游。
- 目标机 `127.0.0.1:8083` 返回 302，`127.0.0.1:5081` 返回 502。
- `/opt/openresty-base` 尚不存在；`/opt/opencode/imgproxy_plus` 和 `/opt/opencode/docker` 已存在。
- APISIX-W 管理接口可访问，当前路由列表为 0；尚未写入路由。
- APISIX-W 地址和密钥仅从目标机环境文件读取用于后续操作，未记录到本文件。

## 2026-08-25 部署前复核

- 目标机 Docker 架构为 `x86_64`，Git 2.34.1，Compose v2.38.2；本机开发环境为 Darwin/Apple Silicon，因此目标机必须按 `linux/amd64` 构建或拉取多架构镜像。
- `opencode-ada` 正常运行且端口映射为 `your-production-host.example:3210->3210/tcp`；3210 绑定在目标机局域网地址而不是 loopback，后续验证应使用 `your-production-host.example:3210`。
- `code-o.ws.example.com` 当前 DNS 解析到 `your-production-ip.example`，与目标机地址不同，需通过 APISIX-W 路由转发。

## 2026-08-25 网关文件部署

- 已将 GitHub `main` 分支代码克隆到目标机 `/opt/openresty-base`。
- 已将本机 `.env` 和 `data/` 持久化数据复制到目标机；敏感配置不在本文件记录。
- 目标机部署目录独立于现有 `/opt/opencode`，不会覆盖现有应用编排。

## 2026-08-25 构建兼容性

- 首次执行 `scripts/restart_gateway.sh --build` 在构建阶段失败：目标机 Docker 未提供 `buildx` 子命令。
- 已在项目重启脚本中加入无 buildx 时的 `docker build --platform` 回退；远端部署会使用该修正版脚本继续构建。

## 2026-08-25 网关已启动

- 已按目标机 `linux/amd64` 成功构建 `ghcr.io/yorkane/openresty-base:latest`。
- 网关容器 `openresty-gateway` 已启动，实际网络模式为 host，运行时模板目录挂载为 `/opt/openresty-base/conf`。
- 重启脚本实测通过：HTTP 登录页 200、未认证 session API 401、Admin 入口 302、HTTPS 登录页 200。

## 2026-08-25 直连验证

- 目标机现有 `opencode-ada` 的 `http://your-production-host.example:3210/` 返回 302，已恢复可用；此前使用 `127.0.0.1:3210` 失败是因为端口只绑定在目标机局域网地址。
- 新网关 HTTP/HTTPS 登录页均返回 200。
- 复制的 `s2-m.ws.example.com` 绑定经网关访问返回 302，说明网关已能到达目标机 3210 上游并进入认证流程。
